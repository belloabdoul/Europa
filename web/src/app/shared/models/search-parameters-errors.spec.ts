import { SearchParametersErrors } from './search-parameters-errors';

describe('SearchParametersErrors', () => {
  it('should create an instance', () => {
    expect(new SearchParametersErrors()).toBeTruthy();
  });
});
